const logic = require('./logic')
const nodeUrl = require('url')

module.exports = (url) => {
    let l = logic(url);
    if (!l.sku) {
        console.log('Supplier logic file has no SKU function: ', url);
        return () => { return null; };
    } else {
        return l.sku(url);
    }
}
