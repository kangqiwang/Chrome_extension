var should = require('should');
var Structure = require('../lib/structure.js')

describe('replaceWithwebAddress', function() {

    this.timeout(10000);

    describe('In Stock Page', function() {

        var payload = {
            "pageType" : "product",
            "url" : "http://www.stage.com/store/product/nfl-dallas-cowboys-prescott-tee/365758/"
        }

        it('should be true when product is in stock', function (done) {

            Structure(payload, function(inStockPage) {
                var expected = JSON.stringify({
                    "name": "NFL Dallas Cowboys Prescott Tee",
                    "price": "24.98",
                    "url": null,
                    "inStock": true,
                    "stockLevel": null,
                    "image": 'http://s7d2.scene7.com/is/image/SpecialtyRetailers/YM-365758-CF-251466-B?fit=constrain,1&wid=500&hei=500&fmt=jpg&resmode=sharp2&op_usm=1,0.4&qlt=80,1',
                    "country": 'us',
                    "description": 'DescriptionShow your love for your team! This t-shirt is perfect for game day or just running around town. Short-sleevesCrewneckFront "Cowboys 4" wording detailBack "Prescott 4" wording detailSolid color navy100% cottonMachine washableImported'
                })
                JSON.stringify(inStockPage).should.equal(expected);
                done()
            })
        });

    });

    /*describe('Out of Stock Page', function() {

        var payload = {
            "pageType" : "product",
            "url" : ""
        }

        it('should be false when product is in store only', function (done) {

            Structure(payload, function(inStockPage) {
                var expected = JSON.stringify({
                    "name": "",
                    "price": "",
                    "url": null,
                    "inStock": false,
                    "stockLevel": null,
                    "image": '',
                    "country": '',
                    "description": ''
                })
                JSON.stringify(inStockPage).should.equal(expected);
                done()
            })
        });

    });

    describe('Search page pagination', function() {

        var payload = {
            "pageType" : "pagination",
            "url" : ""
        }

        it('should be true when number of items and pages correct', function (done) {

            Structure(payload, function(pagination) {
                var expected = JSON.stringify({
                    "items": "",
                    "pages": ""
                })
                JSON.stringify(pagination).should.equal(expected);
                done()
            })
        });

    })*/

    describe('Search page category', function() {

        var payload = {
            "pageType" : "category",
            "url" : "http://www.stage.com/store/category/men/t-shirts/25/"
        }

        it('should be true when number of items and pages correct', function (done) {

            Structure(payload, function(category) {
                var expected = JSON.stringify({
                    "name": "T-shirts"
                })
                JSON.stringify(category).should.equal(expected);
                done()
            })
        });

    });

    describe('Search Page', function() {

        var payload = {
            "pageType" : "search",
            "url" : "http://www.stage.com/store/category/men/t-shirts/25/"
        }

        // Change to be the number of products on the page
        var x = 141;
        it('should return ' + x + ' numer of products', function (done) {
            Structure(payload, function(result) {
                result.length.should.equal(x);
                done()
            })
        });

        var firstProductName = 'NFL Dallas Cowboys Prescott Tee';
        it('first product should be ' + firstProductName, function(done) {
            Structure(payload, function(result) {
                var first = result.shift()
                var expected = JSON.stringify({
                    barcode: null,
                    name: firstProductName,
                    price: '24.98',
                    url: 'http://www.stage.com/store/product/nfl-dallas-cowboys-prescott-tee/365758',
                    image: 'http://s7d2.scene7.com/is/image/SpecialtyRetailers/YM-365758-CF-251466-B?$cdp$',
                    country: 'us'
                })
                JSON.stringify(first).should.equal(expected)
                done()
            })

        })

        var lastProductName = 'Sandlot You\'re Killing Me Smalls';
        it('last product should be ' + lastProductName, function (done) {
            Structure(payload, function(result) {
                var last = result.pop()
                var expected = JSON.stringify({
                    barcode: null,
                    name: lastProductName,
                    price: '9.98',
                    url: 'http://www.stage.com/store/product/sandlot-youre-killing-me-smalls/201820',
                    image: 'http://s7d2.scene7.com/is/image/SpecialtyRetailers/YM-201820-CF-72513-B?$cdp$',
                    country: 'us'
                })

                JSON.stringify(last).should.equal(expected)
                done()
            });
        })

    });

});
