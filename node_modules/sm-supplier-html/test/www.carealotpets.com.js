var should = require('should');
var Structure = require('../lib/structure.js')

describe('replaceWithwebAddress', function() {

    this.timeout(10000);

    describe('In Stock Page', function() {

        var payload = {
            "pageType" : "product",
            "url" : "http://www.carealotpets.com/products/exclusively-dog-wafer-cookies-vanilla-8-oz__26331.aspx"
        }

        it('should be true when product is in stock', function (done) {

            Structure(payload, function(inStockPage) {
                var expected = JSON.stringify({
                    "name": "Exclusively Dog® Wafer Cookies Vanilla 8 oz.",
                    "price": "3.49",
                    "url": null,
                    "inStock": true,
                    "stockLevel": null,
                    "image": 'http://www.carealotpets.com//thumb.aspx?size=280&path=Images%2fsite_images%2f26331lg.jpg',
                    "country": 'us'
                })
                JSON.stringify(inStockPage).should.equal(expected);
                done()
            })
        });

    });

    // describe('Out of Stock Page', function() {

    //     var payload = {
    //         "pageType" : "product",
    //         "url" : ""
    //     }

    //     it('should be false when product is in store only', function (done) {

    //         Structure(payload, function(inStockPage) {
    //             var expected = JSON.stringify({
    //                 "name": "",
    //                 "price": "",
    //                 "url": null,
    //                 "inStock": false,
    //                 "stockLevel": null
    //             })
    //             JSON.stringify(inStockPage).should.equal(expected);
    //             done()
    //         })
    //     });

    // });

    describe('Search page pagination', function() {

        var payload = {
            "pageType" : "pagination",
            "url" : "http://www.carealotpets.com/dog-supplies-top-sellers.aspx"
        }

        it('should be true when number of items and pages correct', function (done) {

            Structure(payload, function(pagination) {
                var expected = JSON.stringify({
                    "items": 62,
                    "pages": 55
                })
                JSON.stringify(pagination).should.equal(expected);
                done()
            })
        });

    })

    describe('Search page category', function() {

        var payload = {
            "pageType" : "category",
            "url" : "http://www.carealotpets.com/dog-supplies-top-sellers.aspx"
        }

        it('should be true when number of items and pages correct', function (done) {

            Structure(payload, function(category) {
                var expected = JSON.stringify({
                    "name": "Dog Supplies"
                })
                JSON.stringify(category).should.equal(expected);
                done()
            })
        });

    });

    describe('Search Page', function() {

        var payload = {
            "pageType" : "search",
            "url" : "http://www.carealotpets.com/Departments/Sale.aspx?sortorder=2&page=1"
        }

        // Change to be the number of products on the page
        var x = 36;
        it('should return ' + x + ' number of products', function (done) {
            Structure(payload, function(result) {
                result.length.should.equal(x);
                done()
            })
        });

        var firstProductName = '#30 Medium Wahl Tid-Bit Blade';
        it('first product should be ' + firstProductName, function(done) {
            Structure(payload, function(result) {
                var first = result.shift()
                var expected = JSON.stringify({
                    barcode: null,
                    name: firstProductName,
                    price: '4.50',
                    url: 'http://www.carealotpets.com/Products/30-Medium-Wahl-Tid-Bit-Blade__4272.aspx',
                    image: 'http://www.carealotpets.com/images/site_images/4272lg.jpg',
                    country: 'us'
                })
                JSON.stringify(first).should.equal(expected)
                done()
            })

        })

        var lastProductName = 'WERUVA b.f.f. Best Feline Friend Tuna & Turkey Tickles Recipe in Gravy Pouch 3 oz';
        it('last product should be ' + lastProductName, function (done) {
            Structure(payload, function(result) {
                var last = result.pop()
                var expected = JSON.stringify({
                    barcode: null,
                    name: lastProductName,
                    price: '0.89',
                    url: 'http://www.carealotpets.com/products/weruva-bff-best-feline-friend-tuna-and-turkey-tickles-recipe-in-gravy-pouch-3-oz__i003083.aspx',
                    image: 'http://www.carealotpets.com/images/site_images3/I003083lg.jpg',
                    country: 'us'
                })

                JSON.stringify(last).should.equal(expected)
                done()
            });
        })

    });

});
