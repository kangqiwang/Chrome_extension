var should = require('should');
var Structure = require('../lib/structure.js')

describe('replaceWithwebAddress', function() {


    describe('In Stock Page', function() {

        var payload = {
            "pageType" : "product",
            "url" : "https://www.luckyvitamin.com/p-437540-luckyvitamin-one-daily-men-s-multivitamin-100-tablets#"
        }

        it('should be true when product is in stock', function (done) {

            Structure(payload, function(inStockPage) {
                var expected = JSON.stringify({
                    "name": "LuckyVitamin - One Daily Men's Multivitamin - 100 Tablets",
                    "price": "4.70",
                    "url": null,
                    "inStock": true,
                    "stockLevel": null,
                    "image": 'https://www.luckyvitamin.com/images/Product/medium/127965.jpg?s=True',
                    "country": 'us',
                    "barcode": '048107156107'
                })
                JSON.stringify(inStockPage).should.equal(expected);
                done()
            })
        });

    });

    // describe('Out of Stock Page', function() {

    //     var payload = {
    //         "pageType" : "product",
    //         "url" : ""
    //     }

    //     it('should be false when product is in store only', function (done) {

    //         Structure(payload, function(inStockPage) {
    //             var expected = JSON.stringify({
    //                 "name": "",
    //                 "price": "",
    //                 "url": null,
    //                 "inStock": false,
    //                 "stockLevel": null
    //             })
    //             JSON.stringify(inStockPage).should.equal(expected);
    //             done()
    //         })
    //     });

    // });

    describe('Search Page', function() {

        var payload = {
            "pageType" : "search",
            "url" : "https://www.luckyvitamin.com/c-2017-air-purifiers-and-humidifiers"
        }

        // Change to be the number of products on the page
        var x = 2;
        it('should return ' + x + ' numer of products', function (done) {
            Structure(payload, function(result) {
                result.length.should.equal(x);
                done()
            })
        });

        var firstProductName = 'Attitude Natural Air Purifier Lavender & Eucalyptus - 8 oz.';
        it('first product should be ' + firstProductName, function(done) {
            Structure(payload, function(result) {
                var first = result.shift()
                var expected = JSON.stringify({
                    barcode: null,
                    name: firstProductName,
                    price: '7.06',
                    url: 'https://www.luckyvitamin.com/p-202682-attitude-natural-air-purifier-lavender-eucalyptus-8-oz',
                    image: 'https://www.luckyvitamin.com/images/product/icon/111791.jpg',
                    country: 'us'
                })
                JSON.stringify(first).should.equal(expected)
                done()
            })

        })

        var lastProductName = 'HoMedics myBaby Ultrasonic Cool Mist Humidifier With SoundSpa MYB-W40';
        it('last product should be ' + lastProductName, function (done) {
            Structure(payload, function(result) {
                var last = result.pop()
                var expected = JSON.stringify({
                    barcode: null,
                    name: lastProductName,
                    price: '50.14',
                    url: 'https://www.luckyvitamin.com/p-301953-homedics-mybaby-ultrasonic-cool-mist-humidifier-with-soundspa-myb-w40',
                    image: 'https://www.luckyvitamin.com/images/product/icon/120481.jpg',
                    country: 'us'
                })

                JSON.stringify(last).should.equal(expected)
                done()
            });
        })

    });

     describe('Search page pagination', function() {

        var payload = {
            "pageType" : "pagination",
            "url" : "https://www.luckyvitamin.com/c-1124-mens-multiples"
        }

        it('should be true when number of items and pages correct', function (done) {

            Structure(payload, function(pagination) {
                var expected = JSON.stringify({
                    "items": 24,
                    "pages": 4
                })
                JSON.stringify(pagination).should.equal(expected);
                done()
            })
        });

    });

    describe('Search page category', function() {

        var payload = {
            "pageType" : "category",
            "url" : "https://www.luckyvitamin.com/c-1124-mens-multiples"
        }

        it('should be true when the category is correct', function (done) {

            Structure(payload, function(category) {
                var expected = JSON.stringify({
                    "name": "Mens Multiples"
                })
                JSON.stringify(category).should.equal(expected);
                done()
            })
        });

    });

});
