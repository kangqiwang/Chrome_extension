// const thisPattern = new RegExp(/^((http[s]?|ftp):\/)?\/?([^:\/\s]+)(:([^\/]*))?((\/[\w\/-]+)*\/)([\w\-\.]+[^#?\s]+)(\?([^#]*))?(#(.*))?$/i);

// let result = []
// let numbersOnly = true
// let noWeirdChars = true
// let number = new RegExp(/[0-9]/g)

const run = (payload) => {
    console.log(payload.skuRegex)
    let validatePattern = new RegExp(payload.skuRegex)
    let result = payload.url.match(validatePattern)


   if(result) { 
       return result[0]
   } else {
       return false
   }
    // if (validatePattern.test(payload.url)) {
    //     (validatePattern.

    //     result.push(payload.match(isolate))
    // }
   

    // thisPattern.exec(url).map((payload) => {
    //     if (validatePattern.test(url)) {
    //         result.push(payload.match(isolate))
    //     }
    // })

    // if(result.length > 1) {
    //     console.error('MULTI-SKU MATCHES')
    // } else if (result.length == 0) {
    //     console.error('NO SKU MATCHES')
    // } else {
    //     result[0].map((char) => {
    //     if(numbersOnly == true) {
    //         char.test()

    
    //     }
    // }}

}

//module.exports = run(payload)

let sku = run({
    url: "http://www.target.com/p/A-52189185/nintendo-switch-with-neon-blue-and-neon-red-joy-con/-/A-52189185",
    skuRegex: /[0-9]+/
})

console.log(sku)




