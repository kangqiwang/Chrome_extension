var should = require('should');
var Structure = require('../lib/structure.js')

describe('www.robertdyas.co.uk', function() {

    this.timeout(10000);

    describe('In Stock Page', function() {

        var payload = {
            "pageType" : "product",
            "url" : "http://www.robertdyas.co.uk/aves-sugar-radio-alarm-clock"
        }

        it('should be true when product is in stock', function (done) {

            Structure(payload, function(inStockPage) {
                var expected = JSON.stringify({
                    "name": "Aves Sugar DAB Radio Alarm Clock",
                    "price": "24.99",
                    "url": null,
                    "inStock": true,
                    "stockLevel": null,
                    "image": 'http://media.robertdyas.co.uk/media/catalog/product/cache/1/image/9df78eab33525d08d6e5fb8d27136e95/1/7/176653_1.jpg',
                    "country": 'uk'
                })
                JSON.stringify(inStockPage).should.equal(expected);
                done()
            })
        });

    });

    // describe('Out of Stock Page', function() {

    //     var payload = {
    //         "pageType" : "product",
    //         "url" : ""
    //     }

    //     it('should be false when product is in store only', function (done) {

    //         Structure(payload, function(inStockPage) {
    //             var expected = JSON.stringify({
    //                 "name": "",
    //                 "price": "",
    //                 "url": null,
    //                 "inStock": false,
    //                 "stockLevel": null,
    //                 "image": '',
    //                 "country": ''
    //             })
    //             JSON.stringify(inStockPage).should.equal(expected);
    //             done()
    //         })
    //     });

    // });

    describe('Search Page', function() {

        var payload = {
            "pageType" : "search",
            "url" : "http://www.robertdyas.co.uk/electricals/home-electricals/audio/radios"
        }

        // Change to be the number of products on the page
        var x = 27;
        it('should return ' + x + ' numer of products', function (done) {
            Structure(payload, function(result) {
                result.length.should.equal(x);
                done()
            })
        });

        var firstProductName = 'Aves Sugar DAB Radio Alarm Clock';
        it('first product should be ' + firstProductName, function(done) {
            Structure(payload, function(result) {
                var first = result.shift()
                var expected = JSON.stringify({
                    barcode: null,
                    name: firstProductName,
                    price: '19.99',
                    url: 'http://www.robertdyas.co.uk/aves-sugar-radio-alarm-clock',
                    image: 'http://robertdyas.resultspage.com/thumb.php?s=229&f=https%3a%2f%2fwww.robertdyas.co.uk%2fmedia%2fcatalog%2fproduct%2f%2f1%2f7%2f176653_1.jpg',
                    country: 'uk'
                })
                JSON.stringify(first).should.equal(expected)
                done()
            })

        })

        var lastProductName = 'Pure Evoke D4 DAB/FM Digital Radio - Walnut';
        it('last product should be ' + lastProductName, function (done) {
            Structure(payload, function(result) {
                var last = result.pop()
                var expected = JSON.stringify({
                    barcode: null,
                    name: lastProductName,
                    price: '129.99',
                    url: 'http://www.robertdyas.co.uk/pure-evoke-d4-dab-fm-digital-radio-walnut',
                    image: 'http://robertdyas.resultspage.com/thumb.php?s=229&f=https%3a%2f%2fwww.robertdyas.co.uk%2fmedia%2fcatalog%2fproduct%2f%2f1%2f7%2f178117.jpg',
                    country: 'uk'
                })

                JSON.stringify(last).should.equal(expected)
                done()
            });
        })

    });

     describe('Search page pagination', function() {

        var payload = {
            "pageType" : "pagination",
            "url" : "http://www.robertdyas.co.uk/electricals/home-electricals/audio/radios"
        }

        it('should be true when number of items and pages correct', function (done) {

            Structure(payload, function(pagination) {
                var expected = JSON.stringify({
                    "items": 27,
                    "pages": 1
                })
                JSON.stringify(pagination).should.equal(expected);
                done()
            })
        });

    });

    describe('Search page category', function() {

        var payload = {
            "pageType" : "category",
            "url" : "http://www.robertdyas.co.uk/electricals/home-electricals/audio/radios"
        }

        it('should be true when the category is correct', function (done) {

            Structure(payload, function(category) {
                var expected = JSON.stringify({
                    "name": "Radios"
                })
                JSON.stringify(category).should.equal(expected);
                done()
            })
        });

    });


});
