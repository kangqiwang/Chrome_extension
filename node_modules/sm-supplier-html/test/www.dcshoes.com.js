var should = require('should');
var Structure = require('../lib/structure.js')

describe('replaceWithwebAddress', function() {

    this.timeout(10000);

    describe('In Stock Page', function() {

        var payload = {
            "pageType" : "product",
            "url" : "http://www.dcshoes.com/lynx-lite-se-low-top-shoes-ADYS700088.html"
        }

        it('should be true when product is in stock', function (done) {

            Structure(payload, function(inStockPage) {
                var expected = JSON.stringify({
                    "name": "Men's Lynx Lite SE Shoes",
                    "price": "75.00",
                    "url": null,
                    "inStock": true,
                    "stockLevel": null,
                    "image": 'http://static.quiksilver.com/www/store.quiksilver.eu/html/images/catalogs/global/dcshoes-products/all/default/small/adys700088_lynxlitese,p_bn0_frt2.jpg',
                    "country": 'us', // can you add country here ($ = us £ = uk)//
                    "description": 'Ghillie lace loops and Lynx T-toe melded with on-trend details like no-sew technology, UNILITETM rubber outsole pods and Ortholite sockliner pay homage to the Lynx Lite SE\'s athletic skate heritage. This shoe offers the unbeatable comfort of a trainer with the features of all of the iconic details you’ve come to expect from the Lynx family.'
                    // we also need the description //
                })
                JSON.stringify(inStockPage).should.equal(expected);
                done()
            })
        });

    });

    /*describe('Out of Stock Page', function() {

        var payload = {
            "pageType" : "product",
            "url" : ""
        }

        it('should be false when product is in store only', function (done) {

            Structure(payload, function(inStockPage) {
                var expected = JSON.stringify({
                    "name": "",
                    "price": "",
                    "url": null,
                    "inStock": false,
                    "stockLevel": null,
                    "image": '',
                    "country": ''
                })
                JSON.stringify(inStockPage).should.equal(expected);
                done()
            })
        });

    });*/

    describe('Search page pagination', function() {

        var payload = {
            "pageType" : "pagination",
            "url" : "http://www.dcshoes.com/search?q=shoes&sz=60"
        }

        it('should be true when number of items and pages correct', function (done) {

            Structure(payload, function(pagination) {
                var expected = JSON.stringify({
                    "items": 60,
                    "pages": 6
                })
                JSON.stringify(pagination).should.equal(expected);
                done()
            })
        });

    })

    describe('Search page category', function() {

        var payload = {
            "pageType" : "category",
            "url" : "http://www.dcshoes.com/kids-skate-shoes/"
        }

        it('should be true when number of items and pages correct', function (done) {

            Structure(payload, function(category) {
                var expected = JSON.stringify({
                    "name": "DC Shoes"
                })
                JSON.stringify(category).should.equal(expected);
                done()
            })
        });

    });

    describe('Search Page', function() {

        var payload = {
            "pageType" : "search",
            "url" : "http://www.dcshoes.com/search?q=shoes&sz=60"
        }

        // Change to be the number of products on the page
        var x = 60; // maker sure you select the max products to display on the page //
        it('should return ' + x + ' numer of products', function (done) {
            Structure(payload, function(result) {
                result.length.should.equal(x);
                done()
            })
        });

        var firstProductName = "Men's Lynx Lite SE Shoes";
        it('first product should be ' + firstProductName, function(done) {
            Structure(payload, function(result) {
                var first = result.shift()
                var expected = JSON.stringify({
                    barcode: null,
                    name: firstProductName,
                    price: '75.00',
                    url: 'http://www.dcshoes.com/lynx-lite-se-low-top-shoes-ADYS700088.html',
                    image: 'http://static.quiksilver.com/www/store.quiksilver.eu/html/images/catalogs/global/dcshoes-products/all/default/medium-large2/adys700088_lynxlitese,p_bn0_frt2.jpg',
                    country: 'us' // can you add country here ($ = us £ = uk)// 
                })
                JSON.stringify(first).should.equal(expected)
                done()
            })

        })

        var lastProductName = "Men's Wes Kremer 2 S Low Top Shoes";
        it('last product should be ' + lastProductName, function (done) {
            Structure(payload, function(result) {
                var last = result.pop()
                var expected = JSON.stringify({
                    barcode: null,
                    name: lastProductName,
                    price: '75.00',
                    url: 'http://www.dcshoes.com/wes-kremer-2-s-low-top-skate-shoes-888327493732.html',
                    image: 'http://static.quiksilver.com/www/store.quiksilver.eu/html/images/catalogs/global/dcshoes-products/all/default/medium-large2/adys300241_weskremer2s,p_xbbw_frt2.jpg',
                    country: 'us' // can you add country here ($ = us £ = uk)//
                })

                JSON.stringify(last).should.equal(expected)
                done()
            });
        })

    });

});
