var should = require('should');
var Structure = require('../lib/structure.js')

describe('www.zumiez.com', function() {

    this.timeout(10000);

    describe('In Stock Page', function() {

        var payload = {
            "pageType" : "product",
            "url" : "http://www.zumiez.com/real-busenitz-flash-flood-8-125-skateboard-deck.html"
        }

        it('should be true when product is in stock', function (done) {

            Structure(payload, function(inStockPage) {
                var expected = JSON.stringify({
                    "name": "Real Busenitz Flash Flood 8.125\" Skateboard Deck",
                    "price": "51.95",
                    "url": null,
                    "inStock": true,
                    "stockLevel": null,
                    "image": "http://scene7.zumiez.com/is/image/zumiez/pdp_hero/Real-Busenitz-Flash-Flood-8.125%22-Skateboard-Deck-_265050-front.jpg",
                    "country": "us"
                })
                JSON.stringify(inStockPage).should.equal(expected);
                done()
            })
        });

    });

    // describe('Out of Stock Page', function() {

    //     var payload = {
    //         "pageType" : "product",
    //         "url" : ""
    //     }

    //     it('should be false when product is in store only', function (done) {

    //         Structure(payload, function(inStockPage) {
    //             var expected = JSON.stringify({
    //                 "name": "",
    //                 "price": "",
    //                 "url": null,
    //                 "inStock": false,
    //                 "stockLevel": null
    //             })
    //             JSON.stringify(inStockPage).should.equal(expected);
    //             done()
    //         })
    //     });

    // });

    describe('Search Page', function() {

        var payload = {
            "pageType" : "search",
            "url" : "http://www.zumiez.com/skate/skateboard-decks.html"
        }

        // Change to be the number of products on the page
        var x = 60;
        it('should return ' + x + ' numer of products', function (done) {
            Structure(payload, function(result) {
                result.length.should.equal(x);
                done()
            })
        });

        var firstProductName = 'MLB Element Yankees For Life 8.0\" Skateboard Deck';
        it('first product should be ' + firstProductName, function(done) {
            Structure(payload, function(result) {
                var first = result.shift()
                var expected = JSON.stringify({
                    barcode: null,
                    name: firstProductName,
                    price: '59.95',
                    url: 'http://www.zumiez.com/mlb-element-yankees-for-life-8-0-skateboard-deck.html',
                    image: 'http://scene7.zumiez.com/is/image/zumiez/cat_max/MLB-Element-Yankees-For-Life-8.0%26quot%3B--Skateboard-Deck-_267968.jpg',
                    country: 'us'
                })
                JSON.stringify(first).should.equal(expected)
                done()
            })

        })

        var lastProductName = 'Santa Cruz Lion God 40\" Drop Through Longboard Deck';
        it('last product should be ' + lastProductName, function (done) {
            Structure(payload, function(result) {
                var last = result.pop()
                var expected = JSON.stringify({
                    barcode: null,
                    name: lastProductName,
                    price: '89.95',
                    url: 'http://www.zumiez.com/santa-cruz-lion-god-40-longboard-deck.html',
                    image: 'http://scene7.zumiez.com/is/image/zumiez/cat_max/Santa-Cruz-Lion-God-40%26quot%3B--Drop-Through-Longboard-Deck-_208917.jpg',
                    country: 'us'
                })

                JSON.stringify(last).should.equal(expected)
                done()
            });
        })

    });

     describe('Search page pagination', function() {

        var payload = {
            "pageType" : "pagination",
            "url" : "http://www.zumiez.com/skate/skateboard-decks.html"
        }

        it('should be true when number of items and pages correct', function (done) {

            Structure(payload, function(pagination) {
                var expected = JSON.stringify({
                    "items": 60,
                    "pages": 9
                })
                JSON.stringify(pagination).should.equal(expected);
                done()
            })
        });

    });

    describe('Search page category', function() {

        var payload = {
            "pageType" : "category",
            "url" : "http://www.zumiez.com/skate/skateboard-decks.html"
        }

        it('should be true when the category is correct', function (done) {

            Structure(payload, function(category) {
                var expected = JSON.stringify({
                    "name": "Decks"
                })
                JSON.stringify(category).should.equal(expected);
                done()
            })
        });

    });

});
