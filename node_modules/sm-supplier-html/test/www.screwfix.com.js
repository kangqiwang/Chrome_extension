var should = require('should');
var Structure = require('../lib/structure.js')

describe('replaceWithwebAddress', function() {

    this.timeout(10000);

    describe('In Stock Page', function() {

        var payload = {
            "pageType" : "product",
            "url" : "http://www.screwfix.com/p/debox-in-line-connector-box/8692h"
        }

        it('should be true when product is in stock', function (done) {

            Structure(payload, function(inStockPage) {
                var expected = JSON.stringify({
                    "name": "Debox In-Line Connector Box",
                    "price": "0.89",
                    "url": null,
                    "inStock": true,
                    "stockLevel": null,
                    "image": 'http://s7g3.scene7.com/is/image//ae235?src=ae235/8692H_P&$prodImageMedium$',
                    "country": 'uk',
                    "description": 'Easy mounting in-line junction box with 4-pole terminal block suitable for flat and round cable up to 4mm². No tools required due to ratchet clips on box ends, pre-cut lead in cable entry and hand removeable external fixing lugs. Supplied with spare locking clip.'

                })
                JSON.stringify(inStockPage).should.equal(expected);
                done()
            })
        });

    });

    /*describe('Out of Stock Page', function() {

        var payload = {
            "pageType" : "product",
            "url" : ""
        }

        it('should be false when product is in store only', function (done) {

            Structure(payload, function(inStockPage) {
                var expected = JSON.stringify({
                    "name": "",
                    "price": "",
                    "url": null,
                    "inStock": false,
                    "stockLevel": null,
                    "image": '',
                    "country": ''
                })
                JSON.stringify(inStockPage).should.equal(expected);
                done()
            })
        });

    });*/

    describe('Search page pagination', function() {

        var payload = {
            "pageType" : "pagination",
            "url" : "http://www.screwfix.com/search?search=box#page_size=100"
        }

        it('should be true when number of items and pages correct', function (done) {

            Structure(payload, function(pagination) {
                var expected = JSON.stringify({
                    "items": 100,
                    "pages": 4
                })
                JSON.stringify(pagination).should.equal(expected);
                done()
            })
        });

    })

    describe('Search page category', function() {

        var payload = {
            "pageType" : "category",
            "url" : "http://www.screwfix.com/c/building-joinery/construction-equipment/cat830700"
        }

        it('should be true when number of items and pages correct', function (done) {

            Structure(payload, function(category) {
                var expected = JSON.stringify({
                    "name": "Construction Equipment"
                })
                JSON.stringify(category).should.equal(expected);
                done()
            })
        });

    });

    describe('Search Page', function() {

        var payload = {
            "pageType" : "search",
            "url" : "http://www.screwfix.com/search?search=box#page_size=100"
        }

        // Change to be the number of products on the page
        var x = 100;
        it('should return ' + x + ' numer of products', function (done) {
            Structure(payload, function(result) {
                result.length.should.equal(x);
                done()
            })
        });

        var firstProductName = 'Debox In-Line Connector Box';
        it('first product should be ' + firstProductName, function(done) {
            Structure(payload, function(result) {
                var first = result.shift()
                var expected = JSON.stringify({
                    barcode: null,
                    name: firstProductName,
                    price: '0.89',
                    url: 'http://www.screwfix.com/p/debox-in-line-connector-box/8692h',
                    image: 'http://s7g3.scene7.com/is/image/ae235/8692H_P?$autoprodblock$',
                    country: 'uk'
                })
                JSON.stringify(first).should.equal(expected)
                done()
            })

        })

        var lastProductName = 'Perimeter Trunking 2G Mounting Box';
        it('last product should be ' + lastProductName, function (done) {
            Structure(payload, function(result) {
                var last = result.pop()
                var expected = JSON.stringify({
                    barcode: null,
                    name: lastProductName,
                    price: '4.09',
                    url: 'http://www.screwfix.com/p/perimeter-trunking-2g-mounting-box/14707',
                    image: 'http://s7g3.scene7.com/is/image/ae235/14707_P?$autoprodblock$',
                    country: 'uk'
                })

                JSON.stringify(last).should.equal(expected)
                done()
            });
        })

    });

});
