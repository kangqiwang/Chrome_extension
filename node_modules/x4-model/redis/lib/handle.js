var redis = require('redis');
var md5 = require('md5');
var hdl = {};

module.exports = function (config) {

	let dbId = md5(config);

	if(hdl[dbId] === undefined) {
        hdl[dbId] = redis.createClient(config)
	}

    return hdl[dbId]

};