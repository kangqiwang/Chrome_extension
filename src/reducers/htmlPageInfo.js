export default function htmlPageInfo(state = {}, action) {
    // switch(action.type) {
    //     case 'RECIEVE_HTML_PAGE_INFO':
    //         newState  =  Object.assign({}, state);
    //         newState.productNum = action.request.product;

    //        return newState
           
    //     default:
          return state;
    // }
}
