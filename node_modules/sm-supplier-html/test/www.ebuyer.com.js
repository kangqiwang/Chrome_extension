var should = require('should');
var Structure = require('../lib/structure.js')
const cache = require('../lib/cache.js')

describe('www.ebuyer.com', function() {

    this.timeout(10000);

    describe('In Stock Page', function() {

        var payload = {
            "pageType" : "product",
            "url" : "https://www.ebuyer.com/753738-aaa-duracell-batteries-tub-24-aaadurb24t"
        }

        it('should be true when product is in stock', function (done) {

            Structure(payload, function(inStockPage) {
                var expected = JSON.stringify({
                    "name": "Aaa Duracell Batteries Tub 24",
                    "price": "10.02",
                    "url": "https://www.ebuyer.com/753738-aaa-duracell-batteries-tub-24-aaadurb24t",
                    "inStock": 'UNCHECKED',
                    "stockLevel": null,
                    "image": 'https://img.ebyrcdn.net/819810-753738-290.jpg',
                    "country": 'uk'
                })
                JSON.stringify(inStockPage).should.equal(expected);
                done()
            })
        });

    });
    describe('Search page category', function() {

        var payload = {
            "pageType" : "category",
            "url" : "http://www.ebuyer.com/store/Storage/cat/Hard-Drive---External/subcat/-Portable-Hard-Drives"
        }

        it('should be true when number of items and pages correct', function (done) {

            Structure(payload, function(category) {
                var expected = JSON.stringify({
                    "name": "Portable Hard Drives"
                })
                JSON.stringify(category).should.equal(expected);
                done()
            })
        });

    });
    describe('Search page pagination', function() {

        var payload = {
            "pageType" : "pagination",
            "url" : "https://www.ebuyer.com/store/Home-&-Leisure/cat/Batteries/subcat/AAA-Batteries"
        }

        it('should be true when number of items and pages correct', function (done) {

            Structure(payload, function(pagination) {
                var expected = JSON.stringify({
                    "items": 24,
                    "pages": 2
                })
                JSON.stringify(pagination).should.equal(expected);
                done()
            })
        });

    });

    // describe('Out of Stock Page', function() {

    //     var payload = {
    //         "pageType" : "product",
    //         "url" : ""
    //     }

    //     it('should be false when product is in store only', function (done) {

    //         Structure(payload, function(inStockPage) {
    //             var expected = JSON.stringify({
    //                 "name": "",
    //                 "price": "",
    //                 "url": null,
    //                 "inStock": false,
    //                 "stockLevel": null
    //             })
    //             JSON.stringify(inStockPage).should.equal(expected);
    //             done()
    //         })
    //     });

    // });

    describe('Search Page', function() {

        var payload = {
            "pageType" : "search",
            "url" : "https://www.ebuyer.com/store/Storage/cat/Memory-Cards/subcat/SD-----SDHC-Cards",
            "html": cache.load("https://www.ebuyer.com/store/Storage/cat/Memory-Cards/subcat/SD-----SDHC-Cards")
        }

        // Change to be the number of products on the page
        var x = 24;
        it('should return ' + x + ' numer of products', function (done) {
            Structure(payload, function(err, result) {
                result.length.should.equal(x);
                done()
            })
        });

        var firstProductName = 'Toshiba 16GB Exceria N302 SD Card';
        it('first product should be ' + firstProductName, function(done) {
            Structure(payload, function(err, result) {
                var first = result.shift()
                var expected = JSON.stringify({
                    barcode: null,
                    name: firstProductName,
                    price: '7.60',
                    url: 'https://www.ebuyer.com/755362-toshiba-16gb-exceria-n302-sd-card-thn-n302r0160e4',
                    image: 'https://img.ebyrcdn.net/763144-755362-150.jpg',
                    country: 'uk'
                })
                JSON.stringify(first).should.equal(expected)
                done()
            })

        })

        var lastProductName = 'Toshiba 16GB EXCERIA PRO N401 SD Card';
        it('last product should be ' + lastProductName, function (done) {
            Structure(payload, function(err, result) {
                var last = result.pop()
                var expected = JSON.stringify({
                    barcode: null,
                    name: lastProductName,
                    price: '11.49',
                    url: 'https://www.ebuyer.com/755358-toshiba-16gb-exceria-pro-n401-sd-card-thn-n401s0160e4',
                    image: 'https://img.ebyrcdn.net/762121-755358-150.jpg',
                    country: 'uk'
                })

                JSON.stringify(last).should.equal(expected)
                done()
            });
        })

    });

});
