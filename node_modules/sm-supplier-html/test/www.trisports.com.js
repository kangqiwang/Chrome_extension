var should = require('should');
var Structure = require('../lib/structure.js')
const cache = require('../lib/cache')

describe('replaceWithwebAddress', function() {

    this.timeout(10000);

    describe('In Stock Page', function() {

        var payload = {
            "pageType" : "product",
            "url" : "http://www.trisports.com/blackburn-grid-13-multi-tool.html",
            "html": cache.load("http://www.trisports.com/blackburn-grid-13-multi-tool.html")
        }

        it('should be true when product is in stock', function (done) {

            Structure(payload, function(err, inStockPage) {
                var expected = JSON.stringify({
                    "name": "Blackburn Grid 13 Multi-Tool",
                    "price": "19.99",
                    "url": payload.url,
                    "inStock": true,
                    "stockLevel": null,
                    "image": "http://ep.yimg.com/ay/trisports/blackburn-grid-13-multi-tool-27.jpg",
                    "country": "us"
                })
                JSON.stringify(inStockPage).should.equal(expected);
                done()
            })
        });

    });

    // describe('Out of Stock Page', function() {

    //     var payload = {
    //         "pageType" : "product",
    //         "url" : ""
    //     }

    //     it('should be false when product is in store only', function (done) {

    //         Structure(payload, function(inStockPage) {
    //             var expected = JSON.stringify({
    //                 "name": "",
    //                 "price": "",
    //                 "url": null,
    //                 "inStock": false,
    //                 "stockLevel": null
    //             })
    //             JSON.stringify(inStockPage).should.equal(expected);
    //             done()
    //         })
    //     });

    // });

    describe('Search Page', function() {

        var payload = {
            "pageType" : "search",
            "url" : "https://www.trisports.com/search/electronic-shifting-parts",
            "html": cache.load("https://www.trisports.com/search/electronic-shifting-parts")
        }

        // Change to be the number of products on the page
        var x = 24;
        it('should return ' + x + ' numer of products', function (done) {
            Structure(payload, function(err, result) {
                result.length.should.equal(x);
                done()
            })
        });

        var firstProductName = 'Blackburn Wayside Multi-Tool';
        it('first product should be ' + firstProductName, function(done) {
            Structure(payload, function(err, result) {
                var first = result.shift()
                var expected = JSON.stringify({
                    barcode: null,
                    name: firstProductName,
                    price: '34.99',
                    url: 'http://www.trisports.com/blackburn-wayside-multi-tool.html',
                    image: 'http://ep.yimg.com/ay/trisports/blackburn-wayside-multi-tool-45.jpg',
                    country: 'us'
                })
                JSON.stringify(first).should.equal(expected)
                done()
            })

        })

        var lastProductName = 'Park Tool MG-2 Nitrile Mechanic Gloves';
        it('last product should be ' + lastProductName, function (done) {
            Structure(payload, function(err, result) {
                var last = result.pop()
                var expected = JSON.stringify({
                    barcode: null,
                    name: lastProductName,
                    price: '19.99',
                    url: 'http://www.trisports.com/park-tool-mg2-mitrile-mechanic-gloves.html',
                    image: 'http://ep.yimg.com/ay/trisports/park-tool-mg-2-nitrile-mechanic-gloves-58.jpg',
                    country: 'us'
                })

                JSON.stringify(last).should.equal(expected)
                done()
            });
        })

    });

     describe('Search page pagination', function() {

        var payload = {
            "pageType" : "pagination",
            "url" : "http://www.trisports.com/all-triathlon-bicycles.html",
            "html": cache.load("http://www.bigbadtoystore.com/bbts/product.aspx?product=ORI10018&mode=retail")
        }

        it('should be true when number of items and pages correct', function (done) {

            Structure(payload, function(err, pagination) {
                var expected = JSON.stringify({
                    "items": 74,
                    "pages": 1
                })
                JSON.stringify(pagination).should.equal(expected);
                done()
            })
        });

    });

    describe('Search page category', function() {

        var payload = {
            "pageType" : "category",
            "url" : "http://www.trisports.com/all-bars.html",
            "html": cache.load("http://www.bigbadtoystore.com/bbts/product.aspx?product=ORI10018&mode=retail")
        }

        it('should be true when the category is correct', function (done) {

            Structure(payload, function(err, category) {
                var expected = JSON.stringify({
                    "name": "All Handlebars & Aerobars"
                })
                JSON.stringify(category).should.equal(expected);
                done()
            })
        });

    });

});
