module.exports = function getPrice(price) {
    if(price) {
    	price = price.replace(',','');
        price = price.match(/[0-9]+\.[0-9][0-9]/);
    }
    if (price && price.length > 0) {
        price = price[0];
    }
    return price;
}
