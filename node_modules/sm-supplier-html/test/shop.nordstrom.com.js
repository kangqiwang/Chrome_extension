var should = require('should');
var Structure = require('../lib/structure.js')

describe('www.nordstrom.com', function() {

    this.timeout(10000);

    describe('In Stock Page', function() {

        var payload = {
            "pageType" : "product",
            "url" : "http://shop.nordstrom.com/s/kane-unke-trim-fit-military-jacket/4326764?origin=category-personalizedsort&fashioncolor=GREY"
        }

        it('should be true when product is in stock', function (done) {

            Structure(payload, function(inStockPage) {
                var expected = JSON.stringify({
                    "name": "Trim Fit Military Jacket",
                    "price": "119.00",
                    "url": null,
                    "inStock": true,
                    "stockLevel": null,
                    "image" : 'http://testn.imgix.net/Zoom/0/_12664400.jpg?fit=fill&bg=FFF&fm=jpg&q=60&w=380&h=583',
                    "country": 'us'
                })
                JSON.stringify(inStockPage).should.equal(expected);
                done()
            })
        });

    });

   // // describe('Out of Stock Page', function() {

   //   //   var payload = {
   //   //       "pageType" : "product",
   //    //      "url" : ""
   //   //   }

   //      it('should be false when product is in store only', function (done) {

   //          Structure(payload, function(inStockPage) {
   //              var expected = JSON.stringify({
   //                  "name": "",
   //                  "price": "",
   //                  "url": null,
   //                  "inStock": false,
   //                  "stockLevel": null
   //              })
   //              JSON.stringify(inStockPage).should.equal(expected);
   //              done()
   //          })
   //      });

   //  });

    describe('Search Page', function() {

        var payload = {
            "pageType" : "search",
            "url" : "http://shop.nordstrom.com/c/mens-sweatshirts-hoodies-fleece?origin=topnav&cm_sp=Top%20Navigation-_-Men-_-Clothing_Sweatshirts%20%26%20Hoodies"
        }

        // Change to be the number of products on the page
        var x = 66;
        it('should return ' + x + ' numer of products', function (done) {
            Structure(payload, function(result) {
                result.length.should.equal(x);
                done()
            })
        });

        var firstProductName = 'Patagonia \'Better Sweater\' Quarter Zip Pullover';
        it('first product should be ' + firstProductName, function(done) {
            Structure(payload, function(result) {
                var first = result.shift()
                var expected = JSON.stringify({
                    barcode: null,
                    name: firstProductName,
                    price: '99.00',
                    url: 'http://shop.nordstrom.com/s/better-sweater-1-4-zip/4177901?origin=category-personalizedsort&fashioncolor=STONEWASH',
                    image : 'http://nord.imgix.net/Zoom/1/_13168441.jpg?fit=fill&fm=jpg&dpr=2&h=368&w=240&q=30',
                    country: 'us'
                })
                JSON.stringify(first).should.equal(expected)
                done()
            })

        })

        var lastProductName = "RVCA Thomas Thermal Hoodie";
        it('last product should be ' + lastProductName, function (done) {
            Structure(payload, function(result) {
                var last = result.pop()
                var expected = JSON.stringify({
                    barcode: null,
                    name: lastProductName,
                    price: '55.00',
                    url: 'http://shop.nordstrom.com/s/rvca-thomas-thermal-hoodie/4479364?origin=category-personalizedsort&fashioncolor=BLACK',
                    image: 'http://nord.imgix.net/Zoom/11/_13316071.jpg?fit=fill&fm=jpg&dpr=2&h=368&w=240&q=30',
                    country: 'us'
                })

                JSON.stringify(last).should.equal(expected)
                done()
            });
        })

    });
     describe('Search page pagination', function() {

        var payload = {
            "pageType" : "pagination",
            "url" : "http://shop.nordstrom.com/c/mens-sweatshirts-hoodies-fleece?origin=topnav&cm_sp=Top%20Navigation-_-Men-_-Clothing_Sweatshirts%20%26%20Hoodies"
        }

        it('should be true when number of items and pages correct', function (done) {

            Structure(payload, function(pagination) {
                var expected = JSON.stringify({
                    "items": 66,
                    "pages": 9
                })
                JSON.stringify(pagination).should.equal(expected);
                done()
            })
        });

    });

    describe('Search page category', function() {

        var payload = {
            "pageType" : "category",
            "url" : "http://shop.nordstrom.com/c/mens-crewneck-sweatshirts?origin=leftnav&cm_sp=Left%20Navigation-_-Crewneck"
        }

        it('should be true when the category is correct', function (done) {

            Structure(payload, function(category) {
                var expected = JSON.stringify({
                    "name": "Crewneck"
                })
                JSON.stringify(category).should.equal(expected);
                done()
            })
        });

    });

});
