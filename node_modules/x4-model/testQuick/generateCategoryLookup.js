const CategoryLookup = require('../index').cache.category

CategoryLookup.generate({
    mongo: {
        db: 'sm',
        host: "127.0.0.1",
        port: 27017
    },
    redis: {
        host: "127.0.0.1",
        port: 6379
    }    
}, () => {
    CategoryLookup.getName('us','apparel_display_on_website', (err, data) => {
        console.log(err)
        console.log(data)
    })
    
})