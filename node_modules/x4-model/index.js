module.exports = {
    elastic: require('./elastic'),
    bulk: require('./bulk'),
    mongo: require('./mongo'),
    mongoObjectID: require('./mongo/objectId'),
    redis: require('./redis'),
    cache: require('./cache'),
    influx: require('./influx')
}
