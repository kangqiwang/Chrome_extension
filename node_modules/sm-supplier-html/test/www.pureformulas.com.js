var should = require('should');
var Structure = require('../lib/structure.js')

describe('replaceWithwebAddress', function() {

    this.timeout(10000);

    describe('In Stock Page', function() {

        var payload = {
            "pageType" : "product",
            "url" : "https://www.pureformulas.com/vitamin-a-100-softgels-by-pureformulas.html"
        }

        it('should be true when product is in stock', function (done) {

            Structure(payload, function(inStockPage) {
                var expected = JSON.stringify({
                    "name": "Vitamin A - 100 Softgels",
                    "price": "5.96",
                    "url": null,
                    "inStock": true,
                    "stockLevel": null,
                    "image": 'https://i3.pureformulas.net/images/product/large/vitamin-a-100-softgels-by-pureformulas.jpg',
                    "country": 'us',
                    "description": "5.0     Brand:PureFormulasItem Number: PUF1046Serving Size: 1 SoftgelServings per Container: 100Quantity per Container: 100 SoftgelsPrice per Serving: $0.06Price per Unit*: $0.06 per Softgel* Price Per Unit may reflect product's price per capsule, tablet, ounce, fluid ounce, etc."
                })
                JSON.stringify(inStockPage).should.equal(expected);
                done()
            })
        });

    });

    // describe('Out of Stock Page', function() {

    //     var payload = {
    //         "pageType" : "product",
    //         "url" : ""
    //     }

    //     it('should be false when product is in store only', function (done) {

    //         Structure(payload, function(inStockPage) {
    //             var expected = JSON.stringify({
    //                 "name": "",
    //                 "price": "",
    //                 "url": null,
    //                 "inStock": false,
    //                 "stockLevel": null,
    //                 "image": '',
    //                 "country": '',
    //                 "description": ''
    //             })
    //             JSON.stringify(inStockPage).should.equal(expected);
    //             done()
    //         })
    //     });

    // });

    describe('Search page pagination', function() {

        var payload = {
            "pageType" : "pagination",
            "url" : "https://www.pureformulas.com/campaign/default/all-vitamin-A-products.html?view=listing"
        }

        it('should be true when number of items and pages correct', function (done) {

            Structure(payload, function(pagination) {
                var expected = JSON.stringify({
                    "items": 16,
                    "pages": 5
                })
                JSON.stringify(pagination).should.equal(expected);
                done()
            })
        });

    })

    describe('Search Page', function() {

        var payload = {
            "pageType" : "search",
            "url" : "https://www.pureformulas.com/campaign/default/all-vitamin-A-products.html?view=listing"
        }

        // Change to be the number of products on the page
        var x = 16;
        it('should return ' + x + ' numer of products', function (done) {
            Structure(payload, function(result) {
                result.length.should.equal(x);
                done()
            })
        });

        var firstProductName = 'Vitamin A - 100 Softgels';
        it('first product should be ' + firstProductName, function(done) {
            Structure(payload, function(result) {
                var first = result.shift()
                var expected = JSON.stringify({
                    barcode: null,
                    name: firstProductName,
                    price: '5.96',
                    url: 'www.pureformulas.com/vitamin-a-100-softgels-by-pureformulas.html',
                    image: 'https://i3.pureformulas.net/images/product/small/vitamin-a-100-softgels-by-pureformulas.jpg',
                    country: 'us'
                })
                JSON.stringify(first).should.equal(expected)
                done()
            })

        })

        var lastProductName = 'Vitamin A Mulsion - 1 fl. oz (30 ml)';
        it('last product should be ' + lastProductName, function (done) {
            Structure(payload, function(result) {
                var last = result.pop()
                var expected = JSON.stringify({
                    barcode: null,
                    name: lastProductName,
                    price: '20.00',
                    url: 'www.pureformulas.com/vitamin-a-mulsion-1-oz-by-professional-formulas.html',
                    image: 'https://i3.pureformulas.net/images/product/small/vitamin-a-mulsion-1-oz-by-professional-formulas.jpg',
                    country: 'us'
                })

                JSON.stringify(last).should.equal(expected)
                done()
            });
        })

    });

     describe('Search page category', function() {

        var payload = {
            "pageType" : "category",
            "url" : "https://www.pureformulas.com/campaign/default/all-vitamin-A-products.html?view=listing"
        }

        it('should be true when the category is correct', function (done) {

            Structure(payload, function(category) {
                var expected = JSON.stringify({
                    "name": "Shop All Vitamin A"
                })
                JSON.stringify(category).should.equal(expected);
                done()
            })
        });

    });

});
