var should = require('should');
var Structure = require('../lib/structure.js')

describe('www.halfords.com', function() {

    this.timeout(10000);

    describe('In Stock Page', function() {

        var payload = {
            "pageType" : "product",
            "url" : "http://www.halfords.com/motoring/car-accessories/car-covers-tarpaulins/halfords-car-cover-medium"
        }

        it('should be true when product is in stock', function (done) {

            Structure(payload, function(inStockPage) {
                var expected = JSON.stringify({
                    "name": "Halfords Car Cover - Medium",
                    "price": "34.99",
                    "url": null,
                    "inStock": true,
                    "stockLevel": null,
                    "image": 'http://i1.adis.ws/i/washford/327251?$pd_main_v2$',
                    "country": 'uk'
                })
                JSON.stringify(inStockPage).should.equal(expected);
                done()
            })
        });

    });

    // describe('Out of Stock Page', function() {

    //     var payload = {
    //         "pageType" : "product",
    //         "url" : ""
    //     }

    //     it('should be false when product is in store only', function (done) {

    //         Structure(payload, function(inStockPage) {
    //             var expected = JSON.stringify({
    //                 "name": "",
    //                 "price": "",
    //                 "url": null,
    //                 "inStock": false,
    //                 "stockLevel": null,
    //                 "image": '',
    //                 "country": ''
    //             })
    //             JSON.stringify(inStockPage).should.equal(expected);
    //             done()
    //         })
    //     });

    // });

    describe('Search Page', function() {

        var payload = {
            "pageType" : "search",
            "url" : "http://www.halfords.com/motoring/car-accessories/car-covers-tarpaulins?cm_re=mmmotoring-_-Caraccessories-_-Carcoverstarpulins"
        }

        // Change to be the number of products on the page
        var x = 21;
        it('should return ' + x + ' numer of products', function (done) {
            Structure(payload, function(result) {
                result.length.should.equal(x);
                done()
            })
        });

        var firstProductName = 'Halfords All Weather Windscreen Shield';
        it('first product should be ' + firstProductName, function(done) {
            Structure(payload, function(result) {
                var first = result.shift()
                var expected = JSON.stringify({
                    barcode: null,
                    name: firstProductName,
                    price: '9.99',
                    url: 'http://www.halfords.com/motoring/car-accessories/car-covers-tarpaulins/halfords-all-weather-windscreen-shield',
                    image: 'http://i1.adis.ws/i/washford/915033?$pm_large$',
                    country: 'uk'
                })
                JSON.stringify(first).should.equal(expected)
                done()
            })

        })

        var lastProductName = 'Extra Large Breathable Car Cover';
        it('last product should be ' + lastProductName, function (done) {
            Structure(payload, function(result) {
                var last = result.pop()
                var expected = JSON.stringify({
                    barcode: null,
                    name: lastProductName,
                    price: '30.23',
                    url: 'http://www.halfords.com/motoring/car-accessories/car-covers-tarpaulins/extra-large-breathable-car-cover',
                    image: 'http://i1.adis.ws/i/washford/1472659317046a?$pm_large$',
                    country: 'uk'
                })

                JSON.stringify(last).should.equal(expected)
                done()
            });
        })

    });

    describe('Search page pagination', function() {

        var payload = {
            "pageType" : "pagination",
            "url" : "http://www.halfords.com/motoring/car-accessories/car-covers-tarpaulins?cm_re=mmmotoring-_-Caraccessories-_-Carcoverstarpulins"
        }

        it('should be true when number of items and pages correct', function (done) {

            Structure(payload, function(pagination) {
                var expected = JSON.stringify({
                    "items": 21,
                    "pages": 2
                })
                JSON.stringify(pagination).should.equal(expected);
                done()
            })
        });

    });

     describe('Not a search page', function() {

        var payload = {
            "pageType" : "pagination",
            "url" : "http://www.halfords.com/motoring/car-accessories/car-covers-tarpaulins/halfords-advanced-all-seasons-car-cover-extra-large"
        }

        it('should return null items and pages', function (done) {

            Structure(payload, function(pagination) {
                var expected = JSON.stringify({
                    "items": null,
                    "pages": null
                })
                JSON.stringify(pagination).should.equal(expected);
                done()
            })
        });

    });

    describe('Search page category', function() {

        var payload = {
            "pageType" : "category",
            "url" : "http://www.halfords.com/motoring/car-accessories/car-covers-tarpaulins?cm_re=mmmotoring-_-Caraccessories-_-Carcoverstarpulins"
        }

        it('should be true when the category is correct', function (done) {

            Structure(payload, function(category) {
                var expected = JSON.stringify({
                    "name": "Car Covers & Tarpaulins"
                })
                JSON.stringify(category).should.equal(expected);
                done()
            })
        });

    });

});
