const loadSuppliers = require('./loadSupplier')
const nodeUrl = require('url')

module.exports = (url) => {
    let host = nodeUrl.parse(url).host
    let logic = loadSuppliers(host);
    return logic;
}