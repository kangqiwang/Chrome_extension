var should = require('should');
var Structure = require('../lib/structure.js')
const cache = require('../lib/cache')

describe('replaceWithDomainName', function () {

    this.timeout(5000);

    describe('In Stock Page', function () {

        var payload = {
            "pageType": "product",
            "url": "https://www.towerhobbies.com/cgi-bin/wti0001p?&I=LXJFXY&P=0",
            "html": cache.load("https://www.towerhobbies.com/cgi-bin/wti0001p?&I=LXJFXY&P=0")
        }

        it('should be true when product is in stock', function (done) {

            Structure(payload, function (err, inStockPage) {
                var expected = JSON.stringify({
                    "name": "Blade Scimitar 170 FPV RTF",
                    "price": "499.99",
                    "url": payload.url,
                    "inStock": true,
                    "stockLevel": null,
                    "image": "https://pics.towerhobbies.com/image/b/blhe2082.jpg",
                    "country": 'us',
                })
                JSON.stringify(inStockPage).should.equal(expected);
                done()
            })
        });

    });

    xdescribe('Out of Stock Page', function () {

        var payload = {
            "pageType": "product",
            "url": "",
            "html": cache.load("")
        }

        it('should be false when product is in store only', function (done) {

            Structure(payload, function (err, inStockPage) {
                var expected = JSON.stringify({
                    "name": "",
                    "price": "",
                    "url": payload.url,
                    "inStock": false,
                    "stockLevel": null,
                    "image": '',
                    "country": '',
                    "description": ''
                })
                JSON.stringify(inStockPage).should.equal(expected);
                done()
            })
        });

    });

    describe('Search page category', function() {

        var payload = {
            "pageType" : "category",
            "url" : "https://www.towerhobbies.com/cgi-bin/WTI0297p?C=EB",
            "html": cache.load("https://www.towerhobbies.com/cgi-bin/WTI0297p?C=EB")
        }

        it('should be true when number of items and pages correct', function (done) {

            Structure(payload, function(err, category) {
                var expected = JSON.stringify({
                    "name": "Drones / Multi-Rotors / Quadcopters R/C",
                    "url":'https://www.towerhobbies.com/cgi-bin/WTI0297p?C=EB'
                })
                JSON.stringify(category).should.equal(expected);
                done()
            })
        });

    });

    xdescribe('Search page pagination', function () {

                var payload = {
                    "pageType": "pagination",
                    "url": "",
                    "html": cache.load("")
                }

                it('should be true when number of items and pages correct', function (done) {

                    Structure(payload, function (err, pagination) {
                        var expected = JSON.stringify({
                            "items": '',
                            "pages": ''
                        })
                        JSON.stringify(pagination).should.equal(expected);
                        done()
                    })
                });

            });


    describe('Search Page', function () {

        var payload = {
            "pageType": "search",
            "url": "https://www.towerhobbies.com/cgi-bin/WTI0297p?C=EB",
            "html": cache.load("https://www.towerhobbies.com/cgi-bin/WTI0297p?C=EB")
        }

        // Change to be the number of products on the page
        var x = 45;
        it('should return ' + x + ' numer of products', function (done) {
            Structure(payload, function (err, result) {
                result.length.should.equal(x);
                done()
            })
        });

        var firstProductName = 'Blade Torrent 110 FPV BNF Basic';
        it('first product should be ' + firstProductName, function (done) {
            Structure(payload, function (err, result) {
                var first = result.shift()
                var expected = JSON.stringify({
                    barcode: null,
                    name: firstProductName,
                    price: '129.99',
                    url: 'https://www.towerhobbies.com/cgi-bin/wti0001p?&I=LXHXPC&P=0',
                    image: 'https://pics.towerhobbies.com/imaget/b/tblhe1000.jpg',
                    country: 'us',
                })
                JSON.stringify(first).should.equal(expected)
                done()
            })

        })

        var lastProductName = 'Yuneec USA Typhoon H RTF w/ST16 CGO3+ 1 Battery';
        it('last product should be ' + lastProductName, function (done) {
            Structure(payload, function (err, result) {
                var last = result.pop()
                var expected = JSON.stringify({
                    barcode: null,
                    name: lastProductName,
                    price: '999.99',
                    url: 'https://www.towerhobbies.com/cgi-bin/wti0001p?&I=LXJJWJ&P=0',
                    image: 'https://pics.towerhobbies.com/imaget/y/tyune1071.jpg',
                    country: 'us',
                })

                JSON.stringify(last).should.equal(expected)
                done()
            });
        })

    });

});


