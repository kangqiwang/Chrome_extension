var should = require('should');
var Structure = require('../lib/structure.js')

describe('www.activitytoysdirect.com', function() {

    this.timeout(10000);

    describe('In Stock Page', function() {

        var payload = {
            "pageType" : "product",
            "url" : "http://www.activitytoysdirect.com/step2/clubhouse-climber-active-bright/p722"
        }

        it('should be true when product is in stock', function (done) {

            Structure(payload, function(inStockPage) {
                var expected = JSON.stringify({
                    "name": "Step2 Clubhouse Climber - Active Bright",
                    "price": "799.00",
                    "url": null,
                    "inStock": true,
                    "stockLevel": null,
                    "image": 'http://www.activitytoysdirect.com/product-images/large/722.jpg',
                    "country": 'uk'
                })
                JSON.stringify(inStockPage).should.equal(expected);
                done()
            })
        });

    });
    describe('Search page pagination', function() {

        var payload = {
            "pageType" : "pagination",
            "url" : "https://www.activitytoysdirect.com/search/?q=toys"
        }

        it('should be true when number of items and pages correct', function (done) {

            Structure(payload, function(pagination) {
                var expected = JSON.stringify({
                    "items": 100,
                    "pages": 3
                })
                JSON.stringify(pagination).should.equal(expected);
                done()
            })
        });

    });

    // describe('Out of Stock Page', function() {

    //     var payload = {
    //         "pageType" : "product",
    //         "url" : "http://www.activitytoysdirect.com/house-of-toys/scratchy-and-friends-bead-puzzle/p1526"
    //     }

    //     it('should be false when product is in store only', function (done) {

    //         Structure(payload, function(inStockPage) {
    //             var expected = JSON.stringify({
    //                 "name": "House of Toys Scratchy & Friends Bead Puzzle",
    //                 "price": "14.99",
    //                 "url": null,
    //                 "inStock": false,
    //                 "stockLevel": null
    //             })
    //             JSON.stringify(inStockPage).should.equal(expected);
    //             done()
    //         })
    //     });

    // });

    describe('Search Page', function() {

        var payload = {
            "pageType" : "search",
            "url" : "https://www.activitytoysdirect.com/art-and-crafts/"
        }

        // Change to be the number of products on the page
        var x = 11;
        it('should return ' + x + ' numer of products', function (done) {
            Structure(payload, function(result) {
                result.length.should.equal(x);
                done()
            })
        });

        var firstProductName = 'Step2 Deluxe Art Master Desk';
        it('first product should be ' + firstProductName, function(done) {
            Structure(payload, function(result) {
                var first = result.shift()
                var expected = JSON.stringify({
                    barcode: null,
                    name: firstProductName,
                    price: '89.99',
                    url: 'https://www.activitytoysdirect.com/step2-deluxe-art-master-desk-p740/',
                    image: 'https://content.activitytoysdirect.com/images/products/thumbs/740.jpg',
                    country: 'uk'
                })
                JSON.stringify(first).should.equal(expected)
                done()
            })

        })
        describe('Search page category', function() {

            var payload = {
                "pageType" : "category",
                "url" : "http://www.activitytoysdirect.com/play-centres.asp"
            }

            it('should be true when number of items and pages correct', function (done) {

                Structure(payload, function(category) {
                    var expected = JSON.stringify({
                        "name": "Play Centres"
                    })
                    JSON.stringify(category).should.equal(expected);
                    done()
                })
            });

        });

        var lastProductName = 'Step2 Jumbo Art Easel';
        it('last product should be ' + lastProductName, function (done) {
            Structure(payload, function(result) {
                var last = result.pop()
                var expected = JSON.stringify({
                    barcode: null,
                    name: lastProductName,
                    price: '99.99',
                    url: 'https://www.activitytoysdirect.com/step2-jumbo-art-easel-p1727/',
                    image: 'https://content.activitytoysdirect.com/images/oos.png',
                    country: 'uk'
                })

                JSON.stringify(last).should.equal(expected)
                done()
            });
        })

    });

});
