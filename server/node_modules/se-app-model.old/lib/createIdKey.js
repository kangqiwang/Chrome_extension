module.exports = (id, records) => {
    var result = {};
    if (records && records.length) {
        records.map((record) => {
            result[record[id]] = record;
        });
    }
    return result;
};
