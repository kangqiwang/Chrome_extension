var should = require('should');
var Structure = require('../lib/structure.js')

describe('replaceWithwebAddress', function() {

    this.timeout(10000);

    describe('In Stock Page', function() {

        var payload = {
            "pageType" : "product",
            "url" : "https://www.scan.co.uk/products/265cm-scan-n2j-rj9-phone-adaptor-2x-35mm-trs-jack-(female)-to-rj9-(male)"
        }

        it('should be true when product is in stock', function (done) {

            Structure(payload, function(inStockPage) {
                var expected = JSON.stringify({
                    "name": "PC Headset Dual 3.5mm to Phone RJ9 Adapter Cable",
                    "price": "7.49",
                    "url": null,
                    "inStock": true,
                    "stockLevel": null,
                    "image": 'https://www.scan.co.uk/images/products/2544355-a.jpg',
                    "country": 'uk',
                    "description": 'Connect a headset of your choice to your home or office phone with this RJ9 adaptor.'
                })
                JSON.stringify(inStockPage).should.equal(expected);
                done()
            })
        });

    });

     describe('Search page category', function() {

        var payload = {
            "pageType" : "category",
            "url" : "https://www.scan.co.uk/search?q=phone"
        }

        it('should be true when number of items and pages correct', function (done) {

            Structure(payload, function(category) {
                var expected = JSON.stringify({
                    "name": "phone"
                })
                JSON.stringify(category).should.equal(expected);
                done()
            })
        });

    });


    /*describe('Out of Stock Page', function() {

        var payload = {
            "pageType" : "product",
            "url" : ""
        }

        it('should be false when product is in store only', function (done) {

            Structure(payload, function(inStockPage) {
                var expected = JSON.stringify({
                    "name": "",
                    "price": "",
                    "url": null,
                    "inStock": false,
                    "stockLevel": null,
                    "image": '',
                    "country": '',
                    "description": ''
                })
                JSON.stringify(inStockPage).should.equal(expected);
                done()
            })
        });

    });*/

    // describe('Search page pagination', function() {

    //     var payload = {
    //         "pageType" : "pagination",
    //         "url" : "https://www.scan.co.uk/search?q=phone"
    //     }

    //     it('should be true when number of items and pages correct', function (done) {

    //         Structure(payload, function(pagination) {
    //             var expected = JSON.stringify({
    //                 "items": 42
    //                 //"pages": ''
    //             })
    //             JSON.stringify(pagination).should.equal(expected);
    //             done()
    //         })
    //     }); // NO PAGINATION NEEDED DUE TO ALL PRODUCTS BEING DISPLAYED ON PAGE //

    // })

    describe('Search Page', function() {

        var payload = {
            "pageType" : "search",
            "url" : "https://www.scan.co.uk/search?q=phone"
        }

        // Change to be the number of products on the page
        var x = 42;
        it('should return ' + x + ' numer of products', function (done) {
            Structure(payload, function(result) {
                result.length.should.equal(x);
                done()
            })
        });

        var firstProductName = '26.5cm Scan N2J RJ9 Phone Adaptor - 2x 3.5mm TRS Jack (Female) to RJ9 (Male)';
        it('first product should be ' + firstProductName, function(done) {
            Structure(payload, function(result) {
                var first = result.shift()
                var expected = JSON.stringify({
                    barcode: null,
                    name: firstProductName,
                    price: '7.49',
                    url: 'https://www.scan.co.uk/products/265cm-scan-n2j-rj9-phone-adaptor-2x-35mm-trs-jack-(female)-to-rj9-(male)',
                    image: 'https://www.scan.co.uk/images/products/largethumbnails/2544355-lt-a.jpg',
                    country: 'uk'
                })
                JSON.stringify(first).should.equal(expected)
                done()
            })

        })

        var lastProductName = 'Microsoft HD-500 Productivity Display Dock, For Windows Phone, DP v1.2/HDMI, 1x USB 3.0 Type-C, 3x USB 2.0, HDCP, Grey';
        it('last product should be ' + lastProductName, function (done) {
            Structure(payload, function(result) {
                var last = result.pop()
                var expected = JSON.stringify({
                    barcode: null,
                    name: lastProductName,
                    price: '51.98',
                    url: 'https://www.scan.co.uk/products/microsoft-hd-500-productivity-display-dock-for-windows-phone-dp-v12-hdmi-1x-usb-30-type-c-3x-usb-20-',
                    image: 'https://www.scan.co.uk/images/products/largethumbnails/2727137-lt-a.jpg',
                    country: 'uk'
                })

                JSON.stringify(last).should.equal(expected)
                done()
            });
        })

    });

});
