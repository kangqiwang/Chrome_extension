var should = require('should');
var Structure = require('../lib/structure.js')

describe('www.plowhearth.com', function() {

    this.timeout(10000);

    describe('In Stock Page', function() {

        var payload = {
            "pageType" : "product",
            "url" : "http://www.plowhearth.com/solar-lighthouse-no-no-bird-feeder.htm"
        }

        it('should be true when product is in stock', function (done) {

            Structure(payload, function(inStockPage) {
                var expected = JSON.stringify({
                    "name": "Solar Lighthouse No-No Bird Feeder",
                    "price": "29.95",
                    "url": null,
                    "inStock": true,
                    "stockLevel": null,
                    "image": 'http://www.plowhearth.com/getDynamicImage.aspx?width=600&height=660&path=59C84_PH0516W_KW59860_20.jpg',
                    "country": 'us'
                })
                JSON.stringify(inStockPage).should.equal(expected);
                done()
            })
        });

    });

    // describe('Out of Stock Page', function() {

    //     var payload = {
    //         "pageType" : "product",
    //         "url" : ""
    //     }

    //     it('should be false when product is in store only', function (done) {

    //         Structure(payload, function(inStockPage) {
    //             var expected = JSON.stringify({
    //                 "name": "",
    //                 "price": "",
    //                 "url": null,
    //                 "inStock": false,
    //                 "stockLevel": null
    //             })
    //             JSON.stringify(inStockPage).should.equal(expected);
    //             done()
    //         })
    //     });

    // });

    describe('Search Page', function() {

        var payload = {
            "pageType" : "search",
            "url" : "http://www.plowhearth.com/bird-feeders-and-squirrel-feeders.htm"
        }

        // Change to be the number of products on the page
        var x = 22;
        it('should return ' + x + ' numer of products', function (done) {
            Structure(payload, function(result) {
                result.length.should.equal(22);
                done()
            })
        });

        var firstProductName = 'Weather-Resistant Squirrel Stopper Bird Feeder And Plant Stand';
        it('first product should be ' + firstProductName, function(done) {
            Structure(payload, function(result) {
                var first = result.shift()
                var expected = JSON.stringify({
                    barcode: null,
                    name: firstProductName,
                    price: '149.95',
                    url: 'http://www.plowhearth.com/weather-resistant-squirrel-stopper-bird-feeder-and-plant-stand.htm',
                    image: 'http://www.plowhearth.com/getDynamicImage.aspx?q=50&width=225&height=248&path=65384x.jpg',
                    country: 'us'
                })
                JSON.stringify(first).should.equal(expected)
                done()
            })

        })

        var lastProductName = 'Picnic Table Squirrel Feeder';
        it('last product should be ' + lastProductName, function (done) {
            Structure(payload, function(result) {
                var last = result.pop()
                var expected = JSON.stringify({
                    barcode: null,
                    name: lastProductName,
                    price: '24.95',
                    url: 'http://www.plowhearth.com/picnic-table-squirrel-feeder.htm',
                    image: 'http://www.plowhearth.com/getDynamicImage.aspx?q=50&width=225&height=248&path=59c77_PH0516W_KW59860_24.jpg',
                    country: 'us'
                })

                JSON.stringify(last).should.equal(expected)
                done()
            });
        })

    });

    describe('Search page pagination', function() {

        var payload = {
            "pageType" : "pagination",
            "url" : "http://www.plowhearth.com/bird-feeders-and-squirrel-feeders.htm"
        }

        it('should be true when number of items and pages correct', function (done) {

            Structure(payload, function(pagination) {
                var expected = JSON.stringify({
                    "items": 24,
                    "pages": 3
                })
                JSON.stringify(pagination).should.equal(expected);
                done()
            })
        });

    });

     describe('Not a search page', function() {

        var payload = {
            "pageType" : "pagination",
            "url" : "http://www.plowhearth.com/solar-lighthouse-no-no-bird-feeder.htm"
        }

        it('should return null items and pages', function (done) {

            Structure(payload, function(pagination) {
                var expected = JSON.stringify({
                    "items": null,
                    "pages": null
                })
                JSON.stringify(pagination).should.equal(expected);
                done()
            })
        });

    });

    describe('Search page category', function() {

        var payload = {
            "pageType" : "category",
            "url" : "http://www.plowhearth.com/bird-feeders-and-squirrel-feeders.htm"
        }

        it('should be true when the category is correct', function (done) {

            Structure(payload, function(category) {
                var expected = JSON.stringify({
                    "name": "Bird Feeders & Squirrel Feeders"
                })
                JSON.stringify(category).should.equal(expected);
                done()
            })
        });

    });

});
