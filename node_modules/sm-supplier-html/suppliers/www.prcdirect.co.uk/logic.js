var PageInfo = require ('../../lib/pageInfo.js')
var SearchPageInfo = require ('../../lib/searchPageInfo.js');
var PaginationNumberInfo = require  ('../../lib/paginationNumberInfo.js');
var CategoryInfo = require ('../../lib/CategoryInfo.js');
var cleanName = require('../../lib/cleanName.js');
var getPrice = require('../../lib/getPrice.js');


module.exports = {
    "useJavascript":true,
    'product' : function($, next) {
        var info  = new PageInfo()
        info.inStock = isStock($)
        info.name = getName($)
        info.price = price($)
        info.image = image($)
        info.country = "uk"
        return info
    },

    'search' : function($, next) {
        // search -  Returns an array of products from the suppliers search page
        var result = []

        Search.container($).map(function(index, product) {
            var info = new SearchPageInfo()
            info.name = Search.name($, product)
            info.price = Search.price($, product);
            info.url = Search.url($, product);
            info.image = Search.image($, product);
            info.country = 'uk';
            result.push(info)
        });

        return result
    },

    'pagination' : function ($, next) {

        var info = new PaginationNumberInfo()
        info.items = Pagination.items($);
        info.pages = Pagination.pages($);

        return info
    },
    'category' : function($, next) {
        var info = new CategoryInfo()
        info.name = Category.name($);

        return info
    }
} 

var isStock = function inStock($) {
    var isInStock = true;
    var availability = $('.tooltip').html() 
    if (availability) {
        if ((availability.indexOf('Out of Stock') > -1)
        || (availability.indexOf('Out of stock') > -1)
        || (availability.indexOf('discontinued') > -1)
        || (availability.indexOf('Not in stock') > -1)
        || (availability.indexOf('Not in Stock') > -1))
        {
            isInStock = false
        }      
    }
    return isInStock
}


var price = function thePrice($) {
    return $('meta[property="og:price:amount"]').attr('content').trim()
}

var getName = function theName($) {   
    return $('.product-name h2').text().trim()
}

var image = function image($) {
    return $('#zoom-btn').attr('href');
}

var Search = {
    container: function($) {
        var searchProducts = $('ul.products-list li.item');
        return searchProducts;

    },
    name: function($, product) {
        var name = $(product).find('div.product-shop-inner h2.product-name a').html();
        name = cleanName(name);
        return name;

    },
    price: function($, product) {
        var price = $(product).find('span.price').html();
        price = getPrice(price);
        return price;

    },
    url: function($, product) {
        var url = null;
        var anchor = $(product).find('a');
        if(anchor) {
            url = $(anchor).attr('href');
        }
        return url;

    },
    image: function($, product) {
        var image = $(product).find('a.product-image img').attr('src');
        image = image;
        return image;

    }
}


var Pagination = {

     items: function($) {
        var items = $('ul.products-list li.item');
        items = items.length;
        return parseInt(items);

     },
     pages: function($) {
        var items = $('ul.products-list li.item');
        items = items.length;

        var products = $('p.amount').text();
        products = products.match(/[0-9]+/g).pop();
       
        sum = products / items
        return Math.ceil(sum)
     }
}

var Category = {
    name: function($) {
        var name = $('div.category-description').text();
        name = cleanName(name);
        return name;
    }
}