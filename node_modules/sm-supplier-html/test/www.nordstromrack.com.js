var should = require('should');
var Structure = require('../lib/structure.js')

describe('replaceWithwebAddress', function() {

    this.timeout(10000);

    describe('In Stock Page', function() {

        var payload = {
            "pageType" : "product",
            "url" : "https://www.nordstromrack.com/shop/product/1720437/tri-coastal-design-finding-dory-150-plus-art-set?color=No+Color"
        }

        it('should be true when product is in stock', function (done) {

            Structure(payload, function(inStockPage) {
                var expected = JSON.stringify({
                    "name": "Tri-Coastal Design Finding Dory 150 Plus Art Set",
                    "price": "16.97",
                    "url": null,
                    "inStock": true,
                    "stockLevel": null,
                    "image": "https://www.hautelookcdn.com/products/10422-70235/large/5461570.jpg",
                    "country": "us"
                })
                JSON.stringify(inStockPage).should.equal(expected);
                done()
            })
        });

    });

    // describe('Out of Stock Page', function() {

    //     var payload = {
    //         "pageType" : "product",
    //         "url" : ""
    //     }

    //     it('should be false when product is in store only', function (done) {

    //         Structure(payload, function(inStockPage) {
    //             var expected = JSON.stringify({
    //                 "name": "",
    //                 "price": "",
    //                 "url": null,
    //                 "inStock": false,
    //                 "stockLevel": null
    //             })
    //             JSON.stringify(inStockPage).should.equal(expected);
    //             done()
    // //         })
    // //     });

    // });

    describe('Search Page', function() {

        var payload = {
            "pageType" : "search",
            "url" : "https://www.nordstromrack.com/shop/Kids/Toys%20&%20Gear"
        }

        // Change to be the number of products on the page
        var x = 99;
        it('should return ' + x + ' numer of products', function (done) {
            Structure(payload, function(result) {
                result.length.should.equal(x);
                done()
            })
        });

        var firstProductName = 'Bead Bazaar Fluffy Soda Pop Rose-A-Licious Scarf Kit';
        it('first product should be ' + firstProductName, function(done) {
            Structure(payload, function(result) {
                var first = result.shift()
                var expected = JSON.stringify({
                    barcode: null,
                    name: firstProductName,
                    price: '14.97',
                    url: 'https://www.nordstromrack.com/shop/product/1792760/bead-bazaar-fluffy-soda-pop-rose-a-licious-scarf-kit?color=ROSE',
                    image: 'http://www.hautelookcdn.com/products/2095/large/5738431.jpg?interpolation=lanczos-none&downsize=233%3A350&output-quality=90',
                    country: 'us'
                })
                JSON.stringify(first).should.equal(expected)
                done()
            })

        })

        var lastProductName = 'Magformers Toys Magformers 30-Piece Set';
        it('last product should be ' + lastProductName, function (done) {
            Structure(payload, function(result) {
                var last = result.pop()
                var expected = JSON.stringify({
                    barcode: null,
                    name: lastProductName,
                    price: '35.97',
                    url: 'https://www.nordstromrack.com/shop/product/1669033/magformers-toys-magformers-30-piece-set?color=No+Color',
                    image: 'http:/bundles/hautelooklayout/images/temp-product.png',
                    country: 'us'                
                })

                JSON.stringify(last).should.equal(expected)
                done()
            });
        })

    });
     describe('Search page pagination', function() {

        var payload = {
            "pageType" : "pagination",
            "url" : "https://www.nordstromrack.com/shop/Kids/Boys"
        }

        it('should be true when number of items and pages correct', function (done) {

            Structure(payload, function(pagination) {
                var expected = JSON.stringify({
                    "items": 99,
                    "pages": 11
                })
                JSON.stringify(pagination).should.equal(expected);
                done()
            })
        });

    });

    describe('Search page category', function() {

        var payload = {
            "pageType" : "category",
            "url" : "https://www.nordstromrack.com/shop/Kids/Boys"
        }

        it('should be true when the category is correct', function (done) {

            Structure(payload, function(category) {
                var expected = JSON.stringify({
                    "name": "Boys' Clothing"
                })
                JSON.stringify(category).should.equal(expected);
                done()
            })
        });

    });

});
